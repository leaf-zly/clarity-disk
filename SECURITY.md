# 安全策略

## 报告漏洞

请不要在公开 Issue 中披露可被利用的漏洞。应使用 GitHub Security Advisory 私密报告，并包含受影响版本、复现步骤、影响范围和建议修复方案。

## 安全边界

- UI 进程不长期持有管理员权限。
- 高权限服务只接受版本化、结构化且经过验证的操作请求。
- 不允许把任意命令行或未校验路径传入高权限服务。
- 系统文件优先由 Windows 官方 API 或受支持工具管理。
- 分区写操作必须记录目标磁盘唯一标识并在执行前重新校验。

## 支持版本

项目进入公开发布后，只维护最新稳定版本和前一个次要版本。当前开发版本不承诺生产支持。
